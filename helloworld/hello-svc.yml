apiVersion: v1
Kind: Service
metatada:
  name: hello
  namespace: helloworld
spec:
  selector:
    app: hello
  type: NodePort
  ports:
    - protocol: TCP
      NodePort: 31000
      port: 80
      targetport: http